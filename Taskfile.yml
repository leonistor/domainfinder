version: 3

tasks:
  build:
    deps:
      - task: build_module
        vars: { NAME: "sprinkle" }
      - task: build_module
        vars: { NAME: "domainify" }
    cmds:
      - echo "build done"
    silent: true

  build_module:
    cmds:
      - go build -o bin/{{.NAME}} {{.NAME}}/main.go
    sources:
      - ./{{.NAME}}/main.go
    generates:
      - ./bin/{{.NAME}}
    method: timestamp
